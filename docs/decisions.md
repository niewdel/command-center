---
tags: [niewdel, command-center, decisions]
---

# Decisions — Command Center

## Decision Log

| # | Date | Decision | Alternatives Considered | Reasoning |
|---|------|----------|------------------------|-----------|
| 1 | 2026-03-25 | Use 7-file project management system | Ad-hoc planning, single doc | Structured approach ensures persistent context across sessions and prevents decision drift |
| 2 | 2026-03-25 | Telegram bot for task capture (not SMS) | Twilio SMS, PWA quick-add only | Free, richer (buttons, voice), Justin already uses Telegram daily |
| 3 | 2026-03-25 | Skip Apple Calendar — Outlook only | Support both, Apple Calendar via CalDAV | Justin switching to Microsoft; CalDAV is complex; reduces scope |
| 4 | 2026-03-25 | Skip calendar integration entirely for V1 | Build with Graph API or ICS fallback | Sandler tenant access uncertain; calendar is not critical path for task tracking MVP; add back later |
| 5 | 2026-03-25 | Single-user, no RBAC for V1 | Build multi-tenant from start | SaaS is a distant maybe; single-user removes massive complexity from every feature |
| 6 | 2026-03-25 | Remove Client Tracker from scope | Build HubSpot → CRM view | Justin doesn't need it; HubSpot integration limited to task sync |
| 7 | 2026-03-25 | Fathom integration via Zapier (not direct API) | Direct API, HubSpot relay, manual paste | No public Fathom API exists; Zapier has official integration with meeting triggers |
| 8 | 2026-03-25 | HubSpot Private App + polling (not webhooks) | OAuth app with webhooks | Private app is simpler; polling every 5 min is fine for personal dashboard; webhooks require OAuth app |
| 9 | 2026-03-25 | grammY for Telegram bot (not Telegraf) | Telegraf, node-telegram-bot-api | TypeScript-first, active development, built-in Next.js webhook adapter |
| 10 | 2026-04-24 | PIN login ALSO signs into Supabase Auth; restore hardened RLS after | Keep permissive RLS forever; route everything through service role | 2026-04-22 "harden_rls" migration (applied via MCP) broke PIN-only app because policies require `auth.uid()`. Migration 015 restored permissive RLS as a stopgap. Migration 016 re-hardens RLS once PIN route calls `signInWithPassword` server-side, giving every device a real Supabase session and a valid `auth.uid()`. |
| 11 | 2026-04-27 | Merge lead-generator + site-audit into command-center as new tabs (not separate Railway services) | Iframe linked deploys; multi-service Railway project; monorepo refactor | Justin wants one platform with paste-URL → audit results inline, no second deploy, no local CLI. Lead-gen is same stack (clean port). Site-audit needs Chromium on Railway via nixpacks.toml (Hobby plan has plenty of RAM). Lead-gen Phase 1 ships first; site-audit Phase 2 next session. |
| 12 | 2026-04-27 | Lead-gen schema preserved as-is (org_id-scoped) with permissive RLS for now | Refactor to user_id directly; merge with workspaces | Keeping the org_id structure means the existing lead-gen CLI engine in ~/lead-generator/ works unchanged after pointing its SUPABASE_URL at command-center's project. Migration 016 re-harden was extended to scope lead tables via organizations.user_id when it eventually runs. |
| 13 | 2026-04-27 | Old lead-gen Supabase project decommissioned (NXDOMAIN); start fresh | Recover data via support; dump from any cached backup | Free-tier Supabase projects are paused after a week of inactivity then deleted. iubbppdaprqmkfuyjcbd.supabase.co no longer resolves. Justin will rerun the CLI engine to repopulate when ready. |
| 14 | 2026-07-01 | Security hardening pass (pre-hacker-review) | Leave permissive policies; rely on login page hiding UI | The public anon key is shipped to the browser; RLS is the only real wall. Five `crm_*` tables (proposals, line items, events, activities, tasks) had `USING(true)` policies for the `public` role — anon could read/write client proposals + pricing directly via the REST API once populated. Replaced with workspace-owner `auth.uid()` policies (server uses service role, so nothing breaks). Also: narrowed the middleware `/api/digest/*` blanket allow-list to only the two self-verifying endpoints (process, telegram) so ingest/reclassify/retry/scrape-trends now require the operator session; made digest process/telegram + all 7 cron routes fail CLOSED on missing secret with a timing-safe compare; pinned function search_paths; made the `audit-reports` bucket private (served only via the auth-gated service-role route). |

| 15 | 2026-07-01 | Tenancy: a workspace IS a tenant, Postgres RLS is the isolation wall (model C) | Parallel `tenants` table; app-layer scoping on the service-role client; per-tenant schemas | Every `crm_*` row already keys off `workspace_id`. Membership-based policies (`is_workspace_member`) mean even a route-layer bug cannot leak across tenants — the DB refuses. Service role survives ONLY where no user session exists (token-gated portal/proposal views, webhooks/cron) plus promote's leads reads. |
| 16 | 2026-07-01 | Reuse `src/lib/supabase-server.ts` as the user-scoped client (spec's `src/lib/supabase/server-user.ts` NOT created) | New file per spec | The cookie-based `@supabase/ssr` server client already existed from the April RLS-recovery work; `getUserScopedClient()` in `src/lib/tenancy/` wraps it. One client construction path, no drift. |
| 17 | 2026-07-01 | `active_workspace` cookie is NOT httpOnly | httpOnly + separate context endpoint for the switcher label | The cookie is a UI hint, not a credential: `resolveActiveWorkspace()` re-validates it against the RLS-visible workspace list on every request, and a tampered value just falls back to the default. Client-side readability lets the switcher label work without an extra fetch. |
| 18 | 2026-07-01 | Login gate = CORE_EMAILS/env allow-list OR ≥1 workspace membership, explicitly fail-closed | Membership-only (drop CORE_EMAILS); JWT custom claims | CORE_EMAILS stays as the lockout-proof fallback (a bad migration or RLS mistake can never lock Justin out). Everyone else needs a `workspace_members` row — admin@ enters purely via its Demo membership. Gate decision extracted to pure `isLoginAllowed()` (5 unit tests) and fails CLOSED on membership-query errors. |
| 19 | 2026-07-01 | Branded Supabase auth emails as repo templates + Management API apply script (not dashboard paste) | Edit in Supabase dashboard by hand | Templates in `supabase/templates/` are reviewable/versioned; `scripts/apply-auth-email-templates.sh` PATCHes the Management API (needs `supabase login`) and optionally swaps auth SMTP to Resend (noreply@niewdel.com) so auth mail stops coming from mail.app.supabase.io. |
| 20 | 2026-07-01 | Agency-internal tool routes gated by role (`agency_admins`), not per-workspace scoping | Workspace-scope every leads/SEO/audit query; hide pages only; leave until client onboarding | Leads, SEO, audits, issues, uploads, and AI commands are cross-workspace agency tooling — scoping them per-workspace fights their design. `requireAgencyAdmin()` (is_agency_admin RPC, fail-closed) on all 31 route files closes the IDOR the membership login gate opened: a Demo/client member now gets 403 on every internal tool while CRM stays membership-scoped via RLS. Nav (sidebar Agents + mobile tabs) hides for non-admins as UX; the 403s are the wall. Probe-verified: unauth 307 / demo member 403 / agency admin 200. |
| 21 | 2026-07-01 | Auth email templates applied via `supabase config push` with auth settings pinned in `supabase/config.toml` | Management API script (needs raw token, blocked); dashboard paste | The CLI is already authenticated but stores its token in the keychain (inaccessible to the apply script). A bare config.toml would RESET live auth settings to CLI defaults (site_url → localhost, signup on, MFA off) — so current production values are pinned in the file and the dry-run diff verified templates-only. One command applies it; the file doubles as versioned documentation of live auth config. |
